test 3:

账号 A 登录
发一条 thought：应能成功写入（数据库 thoughts.author_id = A 的 auth.uid()）。
点赞自己的 thought：应成功（因为允许作者 update）。
在某条 thought 下发一条 comment：应成功写入（comments.author_id = A）。

切换到账号 B 登录
尝试点赞账号 A 的 thought：前端会直接不执行（不会触发 update 请求）。
尝试“修改/删除”账号 A 内容：目前 UI 里似乎没有 edit/delete 操作；若你后续加了，RLS 会挡住。


issues:

1. 作者无法修改自己的thought，注意非作者应该不可以修改

2. 作者无法删除自己的thought，注意非作者不可以修改

3. 非作者应该可以点赞别人的，即使是匿名的也可以；


自测清单（建议你按这个过一遍）

作者 A 登录
打开自己发的 thought -> 应看到“编辑/删除”
编辑保存 -> 刷新后仍是新内容
删除 -> 列表消失
账号 B 登录
打开 A 的 thought -> 不应看到“编辑/删除”
匿名（退出登录）
打开任意 thought -> 点赞应能成功（RPC 生效后）

issues

1. 如果自己未曾点赞，不应该看到爱心是已点赞的状态

2. 测试过程，输入法还没输入完整的中文，键盘enter之后直接发送了，我的输入框的内容还没准备好；


issues:

1. 在自己主页也可以看到“编辑/删除”，最好也有批量删除



issue：
   - 新增加的评论尽可能优先显示在云图中